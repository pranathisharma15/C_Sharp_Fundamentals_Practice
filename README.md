This project is a Product Inventory API built using ASP.NET Core and Entity Framework Core. It includes JWT Authentication and Role‑Based Authorization for secure access control. Users can log in as Admin, Manager, or Viewer, and access API endpoints based on their assigned roles. The API also supports product filtering, sorting, and CRUD operations, with Swagger integrated for testing and token authentication.
